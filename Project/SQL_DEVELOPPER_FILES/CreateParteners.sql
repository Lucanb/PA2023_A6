CREATE TABLE Parteners(
    PartenerID INT NOT NULL,
    Name VARCHAR(50),
    Address VARCHAR(50),
    City VARCHAR(50),
    Country VARCHAR(50),
    Phone VARCHAR(50),
    Email VARCHAR(50),
    CONSTRAINT PK_Parteners PRIMARY KEY (PartenerID)
);